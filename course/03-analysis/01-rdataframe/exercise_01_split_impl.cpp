#include "exercise_01_split_impl.hpp"

int main() {
  // Take the example_04_threadsafe_rng.cpp program and split the
  // implementation of the functions passed to Define operations in the file
  // into a separate header and implementation files. Then, build a shared
  // library with optimizations and link this program against it. In this file,
  // write the RDataFrame computation graph creation and use the functions
  // from the library
  return 0;
}
